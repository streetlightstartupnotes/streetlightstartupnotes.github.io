# Yifan Yang Portfolio

杨一帆的个人作品集网站，使用原生 HTML、CSS 与 JavaScript 构建。

## GitHub Pages

该仓库作为用户主页站点发布，线上地址为：

`https://streetlightstartupnotes.github.io/`

## 后续绑定域名

确定域名后，在仓库根目录添加 `CNAME` 文件，并将域名 DNS 指向 `streetlightstartupnotes.github.io`。GitHub Pages 中启用 HTTPS 后即可完成切换。
